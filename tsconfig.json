{
   "compilerOptions": {
      "target": "ES6", // 출력될 JavaScript 버전
      "module": "commonjs", // 모듈 시스템
      "strict": true, // 엄격한 타입 검사
      "esModuleInterop": true, // ES6 모듈과 호환
      "skipLibCheck": true, // 라이브러리 타입 검사 건너뛰기
      "forceConsistentCasingInFileNames": true, // 파일명 대소문자 일관성 유지
      "outDir": "./dist", // 컴파일된 파일이 출력될 디렉토리
      "rootDir": "./src", // TypeScript 소스 코드의 루트 디렉토리
      "typeRoots": ["./src/types", "./node_modules/@types"], // 타입 선언 경로 추가
      "types": ["node"],
      "moduleResolution": "node" // 모듈 해석 방법
   },
   "include": ["src/**/*.ts"], // src 폴더 내 모든 TypeScript 파일 포함
   "exclude": ["node_modules"] // node_modules 폴더는 제외
}
